{% extends 'base.html.twig' %}

{% block title %}Profile{% endblock %}

{% block body %}
<div class="row">
    <div class="col-12 col-md-8">
        <h1 class="mt-3">Profile</h1>
        <ol class="breadcrumb mb-4">
            <li class="breadcrumb-item"><a href="{{ path('app.home') }}">Dashboard</a></li>
            <li class="breadcrumb-item active">Profile</li>
        </ol>
    </div>
    <div class="col-12 col-md-4">
        <div class="d-flex h-100 align-items-center justify-content-start justify-content-md-end">
            <a href="{{ path('app.home') }}" class="btn btn-light border border-secondary">
                <i class="fa-solid fa-chevron-left"></i>
                Back to dashboard
            </a>
        </div>
    </div>
    <div class="col-12">
        {{ include('profile/_form.html.twig', {'button_label': 'Update'}) }}
    </div>
</div>
{% endblock %}
